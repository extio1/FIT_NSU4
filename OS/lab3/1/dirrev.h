#ifndef MKDIRRES_H
#define MKDIRRES_H 1

/*  Reverse "path_to_directory" directory and
	reverse regular files inside.

	Returns  0 if OK.
	Returns -1 if Error
*/

int mkdirrev(const char* path_to_directory);

#endif
